module std.rng;

trait IsIterator {
}

type Iterator {
}

trait <T> IsRangeOf {
    pub fn length: () -> Int;
    pub fn start: () -> It<T>;
    pub fn end: () -> It<T>;
}

type Policy {
}

pub fn <T> each: ($range: IsRangeOf<T>, action, policy: ?Policy) -> {
    for element: T in range {

    }
}