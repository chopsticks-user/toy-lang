module foo;

import std::io;

local product: (const x: Float, const y: Float) -> Float pure {
    return x * y;
}

export double: (const x: Float) -> Float pure {
    return x: Float -> Float pure { return x |> product(2); };
}

main: () -> Void {
    var x: Int = 5;

    if x > 4 {
        println(x);
    } else if (x > 8) {
        println(x * 2);
    } else {
        println(":(");
    }

    for var i: Int = 0; i < 5; i++ {
        println("{i |> double}");
    }

    return;
}